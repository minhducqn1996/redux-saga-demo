import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  color: {
    primary: '#D32F2F',
    secondary: '#00BCD4',
    error: '#FF5722',
  },
  typography: {
    fontFamily: 'Roboto',
  },
});

export default theme;
